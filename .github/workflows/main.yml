name: Daily Policy AI Check (Kimi)

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: pip install requests
      - name: Run script
        env:
          # 注意：这里要和 GitHub Secrets 里的名字一一对应
          KIMI_API_KEY: sk-dvf9Beh7RIsHZfIRTzmv1kOAEgOsYfziRe9W2FUHqdFNwPa3
          WECOM_WEBHOOK_URL: https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=52b3dac7-fbdd-4f79-85c9-cec274b6151d
        run: python monitor.py
